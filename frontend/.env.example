# Server-side target used by the Next.js /api rewrite.
# The Docker Compose default is http://backend:8000.
BACKEND_URL=http://backend:8000

# Optional browser-visible override for a different backend origin.
# Leave empty to use the same-origin /api rewrite.
NEXT_PUBLIC_API_BASE_URL=